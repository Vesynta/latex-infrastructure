# Context (untracked scratch space)

Assemble material that helps agents reason about a task but must never live in the repository: engineering tickets, supplementary specifications, and reference artefacts (for example a local clone used as a pattern source). A local `.gitignore` keeps everything here out of version control except this README.

Do not submodule clones here.
