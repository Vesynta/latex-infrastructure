# Copy to `.env` and adjust as needed. `.env` is git-ignored.
# After changing `.env`, rebuild the DevContainer.

# Wrike MCP OAuth (Passbolt). Set real values only in gitignored `.env`.
# WRIKE_MCP_CLIENT_ID=
# WRIKE_MCP_CLIENT_SECRET=
