# Agent guidance

Public maintainer image for Vesynta LaTeX builds.
Skills live in `.agents/skills/`. `.claude/skills` is a **symlink**. Catalog: [`.agents/skills/README.md`](.agents/skills/README.md).

If the path is under `.agents/context/`, load `nested-context-work` first. Leftover-ci is `make lint`, `make check-docs`, and `make pre-commit`.
